/-
Released under Apache 2.0 license as described in the file LICENSE.
-/
import Descent.Portability.PortabilityDrift.NonreversibleFlow

/-!
# `Portability.PortabilityDrift` -- the head of a split file

This was 9,208 lines and 555 declarations, the largest file in the corpus by both measures.
It is now nine modules under `PortabilityDrift/`, chained in the order the original was
written, and this file names the last of them -- so every existing
`import Descent.Portability.PortabilityDrift` keeps working and keeps meaning the same
thing.
-/
